package.json
playwright.config.js
tests/
README.md

# Nada de esto tiene que quedar publicado en la web.
# Ojo: .gitignore NO sirve para esto — Cloudflare sube los archivos de la
# carpeta y sólo respeta ESTE archivo.
AUME-propuesta-comercial.pdf
test-results/
playwright-report/
.gitignore
.gitattributes
wrangler.jsonc

# --- Panel de administración ---------------------------------------
# El código del worker corre en el servidor: NO se sirve como archivo.
# Sin esta línea, worker/index.js quedaría descargable desde la web.
worker/
# Documentación interna (incluye cómo está configurado Access).
docs/
node_modules/
.dev.vars
.wrangler/

# admin/ SÍ se publica: son las pantallas del panel. Quien lo protege es
# Cloudflare Access, que corta el acceso antes de servir el archivo.
